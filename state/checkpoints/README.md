# Checkpoints

A checkpoint is the durable unit of progress. It names exact repository heads,
hashes the plan/goal/source lock, lists validation evidence and blockers, and
contains one executable next action.
